# youtube-playlist-exporter
A Python CLI tool to extract YouTube playlist metadata and export data in multiple formats: bare URLs, indexed URLs, bare titles, indexed titles, indexed titles + URLs, and Markdown files with playlist headers.
